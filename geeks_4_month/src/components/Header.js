import React from 'react';
import { Title, Title2 } from './Title';


const Header = () => {
    return (
        <div>
            <Title title={'Header'} name={'Bakyt'}/>
        </div>
    );
};

export default Header;

const sum = (a,b) => {
    return a+b
}

sum(44,55)